export interface TouchSliderConfig {
  // 是否自动轮播
  autoPlay: Boolean;
  // 动画执行时间
  speed: number;
  // 下一张切换时间
  interval: number;
  // 滑动方向
  distance: string;
  // 滑动系数
  slider_dis: number;
  // 是否开启无限轮播
  loop: Boolean;
  // 是否显示指示器
  pointer: Boolean;
}

