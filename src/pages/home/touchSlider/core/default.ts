import { TouchSliderConfig } from '../type';

const defaults: TouchSliderConfig = {
  autoPlay: false,
  speed: 200,
  interval: 3000,
  slider_dis: 50,
  distance: 'level',
  loop: false
};

export default defaults;
