import { TouchSliderConfig } from '../type';

let defaults: TouchSliderConfig = {
  autoPlay: true,
  speed: 200,
  interval: 3000,
  slider_dis: 50,
  distance: 'level',
  loop: false,
  pointer: true
};

export default defaults;
